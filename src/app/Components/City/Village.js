import { Grid } from "@material-ui/core";
import React from "react";

const Village = ({ villages, ceities, setCities }) => {
  return <Grid container justify="center"></Grid>;
};

export default Village;
